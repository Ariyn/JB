#TODO

* more older post list
* category
* upload and compile only changed files
* tag
* rename url
* fix meta tag and title duplication
* change {%%} replacement
* code tag
* empty entire build directory
* list file for dynamic search
	when user type character in search box, js will search this file
* make only 1 parser works
* more good algorithm in searching skin's static files
* hidden keyword
	post will be hidden for list or recently change list or etc... when hidden is in header of post
* change parser.py to parsing language
* make to set charset in config.json
* H1등의 태그 다음에는 p태그가 붙지 않도록.
* Compiler 상속을 받는게 아니라, 모듈을 따로 만들어서 이걸 불러오도록.
* 헤더, 풋터, 사이드 메뉴바 등을 만들어서 컨텐츠 파일을 불러오게.
* {{ 폴더이름 }} 이런식으로 적을경우, 상대경로를 계산해서 변환할것.
* 지금 링크가 html만 붙는데, 이것말고 다른 방식으로도 붙게.
* 404, 403등의 파일 생성.
* 뷰 스크린의 css와 JS 생성.
* 로그 생성
* 링크가 없을경우, 링크색을 다른색으로.
---

#MAYBE

* last updated date variable
* watcher?
---	

#DONE

* run script after compile
* all files need absolute static file directory
* kill sub process
* arg parsing in compiler
* python code for skin(in skin folder)
* posted date variable
* recently changed list variable